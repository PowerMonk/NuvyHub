# NuvyHub

Idrk, might ask the llm to make this better later

# Why is the gitignore ignoring the deno fies???

Because the purpose of this repository is to serve as the proper docker container prep for the server side, since the usage of Deno is specified in the Dockerfile, the previously mentioned files become unnecessary.
